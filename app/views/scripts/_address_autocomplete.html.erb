
<script type="text/javascript">
  var init =
    function( tipoDirParam ){
      autocomplete = null;

      initAutocomplete(tipoDirParam, "<%= autocomplete_for %>");
    };

  // $(document).ready(init( 'address' ));
  $(document).on('page:before-render', init( 'address' ));
</script>
