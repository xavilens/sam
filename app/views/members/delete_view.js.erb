// Si existe el modal lo muestra, si no lo agrega
if($("#member-modal").length > 0){
  $("#member-modal").modal();
}else{
  $('<%= j render "modal", type: "delete", title: "#{@title}", message: "#{@message}" %>').modal();
}
