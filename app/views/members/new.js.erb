// Si existe el modal lo muestra, si no lo agrega
if($("#member-modal").length > 0){
  $("#member-modal").remove();
}

$('<%= j render "modal", title: "#{@title}", type: "new", message: "#{@message}" %>').modal();
