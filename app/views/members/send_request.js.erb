// Si existe el modal lo muestra, si no lo agrega
if($("#member-modal").length > 0){
  $("#member-modal").modal();
}else{
  $('<%= j render "modal", title: "#{@title}", type: "request", message: "#{@message}"  %>').modal();
}
